<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Your first HTML form, styled</title>
    <style>
      form {
        /* Just to center the form on the page */
        margin: 0 auto;
        width: 400px;
        /* To see the outline of the form */
        padding: 1em;
        border: 1px solid #CCC;
        border-radius: 1em;
      }
      form div + div {
        margin-top: 1em;
      }
      label {
        /* To make sure that all labels have the same size and are properly aligned */
        display: inline-block;
        width: 200px;
        text-align: left;
      }
      input, textarea {
        /* To make sure that all text fields have the same font settings By default, textareas have a monospace font */
        font: 1em sans-serif;
        /* To give the same size to all text fields */
        width: 300px;
        box-sizing: border-box; /* To harmonize the look & feel of text field border */
        border: 1px solid #999;
      }
      input:focus, textarea:focus {
        /* To give a little highlight on active elements */
        border-color: #000;
      }
      textarea {
        /* To properly align multiline text fields with their labels */
        vertical-align: top;
        /* To give enough room to type some text */
        height: 5em;
      }
      .button {
        /* To position the buttons to the same position of the text fields */
        padding-left: 90px;
        /* same size as the label elements */
      }
      button {
        /* This extra margin represent roughly the same space as the space between the labels and their text fields */
        margin-left: .5em;
      }
    </style>
</head>


<?php
$id=$_GET['id'];
require 'open-db.php';
	
	$result= mysqli_query($conn, "SELECT * FROM employee WHERE Emp_Number=$id");
	
while ($row = mysqli_fetch_array($result)) {
	
	$e = $row['Emp_Number'];
	$f = $row['First_Name'];
	$l = $row['Last_Name'];
	$a = $row['Address'];
	$b = $row['Birth_Date'];
	$s = $row['Sex'];
	$n = $row['SSN'];
	$d = $row['Start_Date'];
	$j = $row['J_ID'];
	$d = $row['D_ID'];
	$v = $row['V_ID'];
	$p = $row['P_ID'];
?>
	<div class="formPopup" id="updateEmployee"> 
    <form action="updateEmployee.php" method="post">
        <div>
          <label for="number">Employee Number:</label> <input type="text" id="Emp_Number" name="Emp_Number" value="<?php echo htmlspecialchars($d); ?>" readonly="readonly">
        </div>
        <div>
          <label for="fname">First Name:</label> <input type="text" id="First_Name" name="First_Name" value="<?php echo htmlspecialchars($n); ?>" required>
        </div>
        <div>
          <label for="lname">Last Name:</label> <input type="text" id="Last_Name" name="Last_Name" value="<?php echo htmlspecialchars($n); ?>" required>
        </div>
		<div>
          <label for="address">Address:</label> <input type="text" id="Address" name="Address" value="<?php echo htmlspecialchars($n); ?>">
        </div>
        <div>
		  <label for="birth">Birthdate:</label> <input type="text" id="Birth_Date" name="Birth_Date" value="<?php echo htmlspecialchars($a); ?>">
        </div>
 		<div>
          <label for="sex">Sex:</label> <input type="text" id="Sex" name="Sex" value="<?php echo htmlspecialchars($n); ?>">
        </div>
        <div>
		  <label for="SSN">SSN:</label> <input type="text" id="SSN" name="SSN" value="<?php echo htmlspecialchars($a); ?>">
        </div>
		<div>
          <label for="Start_Date">Start Date:</label> <input type="text" id="Start_Date" name="Start_Date" value="<?php echo htmlspecialchars($n); ?>">
        </div>
        <div>
		  <label for="J_ID">Job ID:</label> <input type="text" id="J_ID" name="J_ID" value="<?php echo htmlspecialchars($a); ?>">
        </div>
        <div>
		  <label for="D_ID">Department ID:</label> <input type="text" id="D_ID" name="D_ID" value="<?php echo htmlspecialchars($a); ?>">
        </div>
        <div>
		  <label for="V_ID">Vehicle ID:</label> <input type="text" id="V_ID" name="V_ID" value="<?php echo htmlspecialchars($a); ?>">
        </div>
        <div>
		  <label for="P_ID">Project ID:</label> <input type="text" id="P_ID" name="P_ID" value="<?php echo htmlspecialchars($a); ?>">
        </div>
        <div class="button">
          <button type="submit">Submit</button>
        </div>
    </form>

</html>
<?php } ?>